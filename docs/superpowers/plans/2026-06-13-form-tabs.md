# Form Tabs — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add tab-based form section switching for complex admin forms, with O2M/M2M fields as dedicated tabs with badges, and validate in the OrderFormView demo.

**Architecture:** Five skill reference files get incremental updates (tokens, interactions, components, checklist, generation-rules). The OrderFormView demo is extended with an "attachments" M2M section to trigger the 2+ O2M threshold, converting it from section-card-only to tabs + section cards at desktop. Mobile collapses tabs to flat section cards.

**Tech Stack:** Vue 3 + Element Plus (`ElTabs`, `ElBadge`) + Tailwind CSS

---

### Task 1: Add purple accent token for O2M/M2M section cards

**Files:**
- Modify: `.claude/skills/vue3-element-ui-ux/references/design-tokens.md` (Accent Stripes section)

- [ ] **Step 1: Add purple to the Accent Stripes color table**

In `design-tokens.md`, the Accent Stripes section currently lists blue and cyan. Add purple as a third entry:

```markdown
| Data Category | Stripe Color | Tailwind Class |
|---|---|---|
| Primary / Required | Blue `#2563eb` | `border-l-blue-600` |
| Secondary / Optional | Cyan `#0891b2` | `border-l-cyan-600` |
| Relationship (O2M/M2M) | Purple `#7c3aed` | `border-l-purple-600` |
```

Also add the corresponding title color rule:

```markdown
- Section title color matches stripe: `text-blue-700` for blue, `text-cyan-700` for cyan, `text-purple-700` for purple
```

- [ ] **Step 2: Commit**

```bash
git add .claude/skills/vue3-element-ui-ux/references/design-tokens.md
git commit -m "feat(skill): add purple accent token for O2M/M2M section cards"
```

---

### Task 2: Update tab interaction rules

**Files:**
- Modify: `.claude/skills/vue3-element-ui-ux/references/interaction-rules.md` (Tabs section)

- [ ] **Step 1: Expand the existing Tabs section**

Replace the current Tabs section (lines 134-137):

```
**Tabs:**
- Selected: bottom 2px border + brand-600 text
- Hover: text shifts to brand-600
- No content jump on tab switch
```

With the expanded version:

```markdown
**Tabs:**

- Selected: bottom `2px` border + `brand-600` text + `font-weight: 500`
- Hover: text shifts to `brand-600`
- No content jump on tab switch — panes render instantly
- No validation on tab switch — free navigation between tabs
- Active tab state is local component state (`ref<string>`), not in URL/route
- Scroll position resets to top of tab pane on switch

**Tab Bar in Forms:**

- Tab bar sits between toolbar and form content, using `<el-tabs v-model="activeTab" tab-position="top">`
- All tabs always visible — no horizontal scroll or "more" dropdown. If too many tabs,
  restructure the form rather than adding scrolling.
- Minimum 2 tabs to render the tab bar; fewer = no tabs (section cards only)

**O2M/M2M Tab Badge:**

- Use `<el-badge :value="items.length" :hidden="items.length === 0">` in tab label slot
- Inactive tab badge: `var(--neutral-400)` background
- Active tab badge: `var(--brand-600)` background
- No badge when count is 0

**Validation Error on Hidden Tab:**

- Single submit validates ALL tabs, not just the visible one
- If validation errors exist on a hidden tab:
  - Submit fails
  - Auto-switch to the first tab with errors
  - Error tab label turns red (`var(--danger-600)`) with a `WarningFilled` icon
  - `<el-badge>` on error tab switches to `type="danger"`
- After user fixes errors and re-submits, error indicators clear

**Mobile Tabs:**

- On viewport < 768px, tabs collapse entirely — no tab bar, no dropdown
- Tab bar: `hidden md:flex`
- All section cards render as a flat vertical stack with `gap-4`
- This matches the current non-tab mobile layout behavior
```

- [ ] **Step 2: Commit**

```bash
git add .claude/skills/vue3-element-ui-ux/references/interaction-rules.md
git commit -m "feat(skill): add tabbed form interaction rules — tab bar, badge, error handling, mobile"
```

---

### Task 3: Add TabbedForm to component system mapping

**Files:**
- Modify: `.claude/skills/vue3-element-ui-ux/references/component-system.md` (Composite Components table)

- [ ] **Step 1: Add TabbedForm and FormTab to the Composite Components table**

In `component-system.md`, add two new rows to the Composite Components table:

```markdown
| `TabbedForm` | `TabbedForm` | Form with tab bar for 3+ sections or 2+ O2M/M2M. Uses `<el-tabs>` inside `<el-form>`. Desktop: tabs switch panes. Mobile: flat section cards. |
| `FormTab` | `FormTab` | Single tab pane within a TabbedForm. Can contain 1+ FormSection cards. O2M/M2M tabs get purple accent stripe + `<el-badge>` count. |
```

- [ ] **Step 2: Update the Layout Mapping table**

In the Layout Mapping section, update `admin-form`:

```markdown
| `admin-form` (simple) | `AdminLayout + PageHeader + FormSection + ActionBar` |
| `admin-form` (tabbed) | `AdminLayout + PageHeader + TabbedForm + FormTab[] + FormSection[] + ActionBar` |
```

- [ ] **Step 3: Commit**

```bash
git add .claude/skills/vue3-element-ui-ux/references/component-system.md
git commit -m "feat(skill): add TabbedForm and FormTab to component mapping"
```

---

### Task 4: Add tabbed form check items to review checklist

**Files:**
- Modify: `.claude/skills/vue3-element-ui-ux/references/review-checklist.md` (Section 4: Form Experience)

- [ ] **Step 1: Add tabbed form check items after the existing O2M sub-form items**

After the O2M sub-form check item, insert:

```markdown
- **Tabbed form (when applicable):**
  - Decision matrix applied correctly: tabs only when 3+ logical groups OR 2+ O2M/M2M relationships
  - Tab bar sits between toolbar and form content, not inside a card wrapper
  - Each O2M/M2M relationship has its own dedicated tab with count badge
  - O2M/M2M tab uses purple accent stripe (`border-l-purple-600`) + `text-purple-700` title
  - Tab switching is instant, no validation triggered on switch
  - Single submit validates all tabs; errors on hidden tabs auto-switch + mark tab red
  - Desktop: tab bar visible, one pane at a time
  - Mobile (<768px): tabs collapse to flat vertical section cards, no tab bar, no dropdown
  - No horizontal scroll on tab bar; all tabs fit at once
```

- [ ] **Step 2: Commit**

```bash
git add .claude/skills/vue3-element-ui-ux/references/review-checklist.md
git commit -m "feat(skill): add tabbed form check items to review checklist"
```

---

### Task 5: Add Tabbed Form section to generation rules

**Files:**
- Modify: `.claude/skills/vue3-element-ui-ux/references/generation-rules.md` (after the Three-Mode Form section, around line 470)

- [ ] **Step 1: Add the Tabbed Form section**

Insert after the Three-Mode Form section (after line 470, before the Admin Dashboard section):

```markdown
### Tabbed Form (Multi-Tab Admin Forms)

When a form has 3+ logical field groups OR 2+ O2M/M2M relationships, organize sections
into tabs instead of stacking them all vertically. Simple forms (fewer groups, ≤1 O2M)
continue using section cards only.

**Decision Matrix:**

| Condition | Layout |
|---|---|
| < 3 logical field groups AND ≤ 1 O2M/M2M | Section cards only (current pattern) |
| 3+ logical field groups OR 2+ O2M/M2M | Tabs + section cards |

**Tab Bar:**

```html
<el-tabs
  v-if="tabs.length >= 2"
  v-model="activeTab"
  class="hidden md:block"
  @tab-change="handleTabChange"
>
  <el-tab-pane
    v-for="tab in tabs"
    :key="tab.key"
    :name="tab.key"
  >
    <template #label>
      {{ tab.label }}
      <el-badge
        v-if="tab.count !== undefined"
        :value="tab.count"
        :hidden="tab.count === 0"
        :type="tab.hasError ? 'danger' : 'primary'"
      />
      <el-icon v-if="tab.hasError" class="ml-1" style="color: var(--danger-600)">
        <WarningFilled />
      </el-icon>
    </template>
  </el-tab-pane>
</el-tabs>
```

Key points:
- Tab bar uses `hidden md:block` — visible on desktop only
- `v-if="tabs.length >= 2"` — no tab bar when there's only one tab
- Badge via `<el-badge>` in the `#label` slot; hidden when count is 0
- Error tab: badge `type="danger"` + `WarningFilled` icon in red
- Active tab: `border-bottom: 2px solid var(--brand-600)` + `color: var(--brand-600)`

**Tab Definition:**

```typescript
interface TabDefinition {
  key: string           // unique tab key, e.g., 'basic', 'items', 'attachments'
  label: string         // display label, e.g., '基本信息', '商品清单'
  count?: number        // O2M/M2M item count for badge (undefined = no badge)
  hasError?: boolean    // set to true when hidden tab has validation errors
}

const tabs = computed<TabDefinition[]>(() => [
  { key: 'basic', label: '基本信息' },
  { key: 'delivery', label: '配送 & 备注' },
  { key: 'items', label: '商品清单', count: form.items.length },
  { key: 'attachments', label: '附件', count: form.attachments.length },
])

const activeTab = ref<string>(tabs.value[0]?.key ?? 'basic')
```

**Tab Pane Content (Desktop):**

On desktop, only the active tab's content renders:

```html
<el-form ref="formRef" :model="form" :rules="isView ? {} : rules" label-position="top"
         @submit.prevent="isView ? undefined : handleSubmit()">
  <!-- Desktop: active tab pane only -->
  <div v-if="tabs.length >= 2" class="hidden md:flex flex-col gap-4">
    <template v-for="tab in tabs" :key="tab.key">
      <div v-show="activeTab === tab.key">
        <!-- Tab-specific section cards render here. Each tab pane contains
             1+ section cards with accent stripes, same as the non-tab layout.
             O2M/M2M tabs get purple accent stripe. -->
      </div>
    </template>
  </div>

  <!-- Mobile + Simple forms: flat section cards -->
  <div :class="tabs.length >= 2 ? 'md:hidden flex flex-col gap-4' : 'flex flex-col gap-4'">
    <!-- All section cards rendered flat, same as current non-tab layout -->
  </div>
</el-form>
```

Key points:
- Desktop: `v-show` on each pane (not `v-if`) so form fields stay registered for validation
- Mobile: all sections rendered flat, no tab chrome — uses `md:hidden` when tabs exist, or no wrapper when no tabs
- Section cards inside tabs use the same accent stripe pattern — blue/cyan/purple per content type
- O2M/M2M tab content uses the same O2M sub-form pattern (column headers + data rows)
  but wrapped in a `FormSection` with purple accent stripe

**Validation Across Tabs:**

```typescript
async function handleSubmit() {
  if (!formRef.value) return
  try {
    await formRef.value.validate()
  } catch {
    // Find first tab with errors and switch to it
    const errorFields = Object.keys(formRef.value.fields || {})
    for (const tab of tabs.value) {
      if (tabHasErrors(tab, errorFields)) {
        activeTab.value = tab.key
        tab.hasError = true
        break
      }
    }
    return
  }
  // ... proceed with submission
}

function handleTabChange(key: string) {
  // Clear error state when user switches to a previously-errored tab
  const tab = tabs.value.find(t => t.key === key)
  if (tab) tab.hasError = false
}
```

Key points:
- `formRef.value.validate()` validates all fields regardless of which tab is visible
- On validation failure, find the first tab containing an error field and switch to it
- Set `tab.hasError = true` to show red label + warning icon
- Clear error state when user navigates to the errored tab (they're now looking at the errors)
- `v-show` (not `v-if`) keeps hidden tab fields in the DOM so Element Plus validation can reach them

**O2M/M2M Tab Content:**

Each relationship tab contains a single section card with purple accent stripe:

```html
<!-- Desktop: inside the active tab pane -->
<div class="bg-white rounded-btn border border-neutral-200 border-l-[3px] border-l-purple-600 p-5 md:p-6">
  <div class="text-sm font-semibold text-purple-700 mb-4 uppercase tracking-wide">商品清单</div>
  <!-- Existing O2M sub-form pattern (column headers + data rows) -->
  <div class="mt-4">
    <div class="text-sm text-neutral-600 mb-2">Items</div>
    <!-- Column headers -->
    <div class="hidden md:grid gap-2 px-1 py-2 border-b border-neutral-100 mb-2"
         style="grid-template-columns: 2fr 1fr 80px 1fr 100px 40px">
      <span class="text-xs text-neutral-400">Name *</span>
      <span class="text-xs text-neutral-400">Spec</span>
      <span class="text-xs text-neutral-400 text-center">Qty</span>
      <span class="text-xs text-neutral-400 text-right">Price</span>
      <span class="text-xs text-neutral-400 text-right">Subtotal</span>
      <span></span>
    </div>
    <!-- Data rows (same as existing O2M pattern) -->
    ...
    <el-button link type="primary" :icon="Plus" @click="addItem">Add item</el-button>
  </div>
</div>
```

Key points:
- Reuses the exact O2M sub-form pattern from above — no structural change
- Purple accent and title color signal "this is a relationship, not a field group"
- Will be visible: on desktop when the O2M tab is active; on mobile as a flat section card

**Three-Mode Integration:**

Tabs are identical across create/view/edit. Only content inside panes changes:

| Mode | Tab Bar | Tab Content | O2M Tab |
|---|---|---|---|
| create | Visible | Editable fields + rules | Editable grid, add/delete enabled |
| view | Visible | Read-only text | Read-only grid, no add/delete, no delete column |
| edit | Visible | Editable fields + rules | Editable grid, add/delete enabled |

View mode: tab bar still renders — users can browse all sections of the record.
```

- [ ] **Step 2: Commit**

```bash
git add .claude/skills/vue3-element-ui-ux/references/generation-rules.md
git commit -m "feat(skill): add Tabbed Form section to generation rules — decision matrix, tab bar, validation, O2M tabs"
```

---

### Task 6: Extend OrderFormView with attachments + tab layout

**Files:**
- Modify: `login-homepage-preview/src/views/OrderFormView.vue`

- [ ] **Step 1: Add attachments to the data model and form state**

In the `<script setup>` section, after the existing `OrderItem` interface and `useI18n()` line, add:

```typescript
// Attachment interface for M2M demo
interface Attachment {
  fileName: string
  fileSize: string
  uploadedAt: string
}

const defaultAttachment = (): Attachment => ({
  fileName: '',
  fileSize: '',
  uploadedAt: new Date().toISOString().slice(0, 10),
})
```

In the `form` reactive object (around line 30), add `attachments` after `items`:

```typescript
const form = reactive({
  customer: '',
  phone: '',
  amount: 0,
  channel: '',
  items: [defaultItem()] as OrderItem[],
  attachments: [defaultAttachment()] as Attachment[],
  address: '',
  deliveryMethod: '',
  remark: '',
})
```

- [ ] **Step 2: Add attachment helper functions**

After `removeItem()`, add:

```typescript
function addAttachment() {
  form.attachments.push(defaultAttachment())
}

function removeAttachment(i: number) {
  if (form.attachments.length > 1) {
    form.attachments.splice(i, 1)
  }
}
```

- [ ] **Step 3: Define the tabs array and activeTab**

After `const isCreate = computed(...)`, add:

```typescript
interface TabDefinition {
  key: string
  label: string
  count?: number
  hasError?: boolean
}

const tabs = computed<TabDefinition[]>(() => [
  { key: 'basic', label: '基本信息' },
  { key: 'delivery', label: '配送 & 备注' },
  { key: 'items', label: '商品清单', count: form.items.length },
  { key: 'attachments', label: '附件', count: form.attachments.length },
])

const activeTab = ref<string>('basic')

function handleTabChange(key: string) {
  const tab = tabs.value.find(t => t.key === key)
  if (tab) tab.hasError = false
}
```

- [ ] **Step 4: Add cross-tab validation logic**

Replace the existing `handleSubmit` function with:

```typescript
async function handleSubmit() {
  if (!formRef.value) return
  submitting.value = true
  try {
    await formRef.value.validate()
  } catch {
    // Find first tab with errors and switch to it
    const fieldNames = formRef.value.fields ? Object.keys(formRef.value.fields) : []
    for (const tab of tabs.value) {
      if (tabHasErrors(tab.key, fieldNames)) {
        activeTab.value = tab.key
        tab.hasError = true
        break
      }
    }
    submitting.value = false
    return
  }
  try {
    const items = form.items
      .filter(item => item.name.trim())
      .map(item => ({
        name: item.name.trim(),
        spec: item.spec.trim(),
        quantity: item.quantity,
        unitPrice: item.unitPrice,
      }))
    const total = items.reduce((sum, item) =>
      sum + itemSubtotal(item), 0
    )
    if (isEdit.value) {
      const i = __mockOrders.findIndex(o => o.id === recordId)
      if (i !== -1) {
        Object.assign(__mockOrders[i], {
          customer: form.customer,
          phone: form.phone,
          amount: total,
          channel: form.channel,
          items,
          address: form.address,
          deliveryMethod: form.deliveryMethod,
          remark: form.remark,
        })
      }
      ElMessage.success('保存成功')
    } else {
      __mockOrders.push({
        id: `ORD-${Date.now()}`,
        customer: form.customer,
        phone: form.phone,
        amount: total,
        channel: form.channel,
        items,
        status: 'pending' as const,
        createdAt: new Date().toISOString().slice(0, 10),
        address: form.address,
        deliveryMethod: form.deliveryMethod,
        remark: form.remark,
      })
      ElMessage.success('创建成功')
    }
    router.push('/admin/orders')
  } finally {
    submitting.value = false
  }
}

// Helper: check if a tab contains any field with errors
function tabHasErrors(tabKey: string, errorFields: string[]): boolean {
  // Map tab keys to their field names
  const tabFieldMap: Record<string, string[]> = {
    basic: ['customer', 'phone', 'amount', 'channel'],
    delivery: ['deliveryMethod', 'address', 'remark'],
    items: ['items'],
    attachments: ['attachments'],
  }
  const fields = tabFieldMap[tabKey] || []
  return errorFields.some(f => fields.includes(f))
}
```

- [ ] **Step 5: Add the tab bar to the template**

Replace the current `<el-form>` opening (around line 190) and the content between toolbar and form. The toolbar stays the same. After the toolbar `</div>`, insert:

```html
    <!-- Tab Bar (desktop only; shown when tabs >= 2) -->
    <el-tabs
      v-if="tabs.length >= 2"
      v-model="activeTab"
      class="hidden md:block mb-4"
      @tab-change="handleTabChange"
    >
      <el-tab-pane
        v-for="tab in tabs"
        :key="tab.key"
        :name="tab.key"
      >
        <template #label>
          <span :class="tab.hasError ? 'text-red-600' : ''">{{ tab.label }}</span>
          <el-badge
            v-if="tab.count !== undefined"
            :value="tab.count"
            :hidden="tab.count === 0"
            :type="tab.hasError ? 'danger' : 'primary'"
            class="ml-1"
          />
          <el-icon v-if="tab.hasError" class="ml-1" style="color: var(--el-color-danger)">
            <WarningFilled />
          </el-icon>
        </template>
      </el-tab-pane>
    </el-tabs>
```

- [ ] **Step 6: Restructure the form sections into tab panes + flat mobile fallback**

Replace the current form body (the `<el-form>` and its children) with:

```html
    <el-form
      ref="formRef"
      :model="form"
      :rules="isView ? {} : rules"
      label-position="top"
      class="flex flex-col gap-4"
      @submit.prevent="isView ? undefined : handleSubmit()"
    >
      <!-- ===== DESKTOP: Tab Panes (one active at a time) ===== -->
      <template v-if="tabs.length >= 2">
        <!-- Tab: 基本信息 -->
        <div v-show="activeTab === 'basic'" class="hidden md:flex flex-col gap-4">
          <div class="bg-white rounded-btn border border-neutral-200 border-l-[3px] border-l-blue-600 p-5 md:p-6">
            <div class="text-sm font-semibold text-blue-700 mb-4 uppercase tracking-wide">基本信息</div>
            <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
              <el-form-item label="客户名称" prop="customer">
                <template v-if="isView"><div class="text-sm text-neutral-950 pt-1">{{ form.customer }}</div></template>
                <el-input v-else v-model="form.customer" placeholder="请输入客户名称" />
              </el-form-item>
              <el-form-item label="手机号" prop="phone">
                <template v-if="isView"><div class="text-sm text-neutral-950 pt-1">{{ form.phone }}</div></template>
                <el-input v-else v-model="form.phone" placeholder="请输入手机号" />
              </el-form-item>
              <el-form-item label="订单金额" prop="amount">
                <template v-if="isView"><div class="text-sm text-neutral-950 pt-1">{{ $n(form.amount, 'currency') }}</div></template>
                <el-input-number v-else v-model="form.amount" :min="0" :precision="2" controls-position="right" class="w-full" />
              </el-form-item>
            </div>
            <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mt-4">
              <el-form-item label="渠道">
                <template v-if="isView"><div class="text-sm text-neutral-950 pt-1">{{ form.channel }}</div></template>
                <el-select v-else v-model="form.channel" placeholder="请选择渠道" class="w-full">
                  <el-option label="线上" value="线上" />
                  <el-option label="门店" value="门店" />
                  <el-option label="渠道商" value="渠道商" />
                </el-select>
              </el-form-item>
            </div>
          </div>
        </div>

        <!-- Tab: 配送 & 备注 -->
        <div v-show="activeTab === 'delivery'" class="hidden md:flex flex-col gap-4">
          <div class="bg-white rounded-btn border border-neutral-200 border-l-[3px] border-l-cyan-600 p-5 md:p-6">
            <div class="text-sm font-semibold text-cyan-700 mb-4 uppercase tracking-wide">配送 & 备注</div>
            <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
              <el-form-item label="配送方式">
                <template v-if="isView"><div class="text-sm text-neutral-950 pt-1">{{ form.deliveryMethod }}</div></template>
                <el-select v-else v-model="form.deliveryMethod" placeholder="请选择配送方式" class="w-full">
                  <el-option label="快递" value="快递" />
                  <el-option label="自提" value="自提" />
                  <el-option label="同城送" value="同城送" />
                </el-select>
              </el-form-item>
            </div>
            <el-form-item label="配送地址" class="mt-4">
              <template v-if="isView"><div class="text-sm text-neutral-950 whitespace-pre-wrap pt-1">{{ form.address || '-' }}</div></template>
              <el-input v-else v-model="form.address" type="textarea" :rows="2" placeholder="请输入配送地址" />
            </el-form-item>
            <el-form-item label="备注" class="mt-4">
              <template v-if="isView"><div class="text-sm text-neutral-950 whitespace-pre-wrap pt-1">{{ form.remark || '-' }}</div></template>
              <el-input v-else v-model="form.remark" type="textarea" :rows="2" placeholder="备注信息（可选）" />
            </el-form-item>
          </div>
        </div>

        <!-- Tab: 商品清单 (O2M) -->
        <div v-show="activeTab === 'items'" class="hidden md:flex flex-col gap-4">
          <div class="bg-white rounded-btn border border-neutral-200 border-l-[3px] border-l-purple-600 p-5 md:p-6">
            <div class="text-sm font-semibold text-purple-700 mb-4 uppercase tracking-wide">商品清单</div>
            <div class="mt-4">
              <div class="text-sm text-neutral-600 mb-2">Items</div>
              <!-- Column headers (desktop only) -->
              <div class="hidden md:grid gap-2 px-1 py-2 border-b border-neutral-100 mb-2"
                   style="grid-template-columns: 2fr 1fr 80px 1fr 100px 40px">
                <span class="text-xs text-neutral-400">Name *</span>
                <span class="text-xs text-neutral-400">Spec</span>
                <span class="text-xs text-neutral-400 text-center">Qty</span>
                <span class="text-xs text-neutral-400 text-right">Price</span>
                <span class="text-xs text-neutral-400 text-right">Subtotal</span>
                <span></span>
              </div>
              <!-- Data rows -->
              <template v-if="isView">
                <div v-for="(item, i) in form.items" :key="i"
                     class="grid gap-2 items-center py-2 border-b border-neutral-50 last:border-0"
                     style="grid-template-columns: 2fr 1fr 80px 1fr 100px">
                  <div class="text-sm text-neutral-950">{{ item.name || '-' }}</div>
                  <div class="text-sm text-neutral-500">{{ item.spec || '-' }}</div>
                  <div class="text-sm text-neutral-950 text-center">{{ item.quantity }}</div>
                  <div class="text-sm text-neutral-950 text-right">{{ $n(item.unitPrice, 'currency') }}</div>
                  <div class="text-sm font-semibold text-neutral-950 text-right">{{ $n(itemSubtotal(item), 'currency') }}</div>
                </div>
              </template>
              <template v-else>
                <div v-for="(item, i) in form.items" :key="i"
                     class="grid gap-2 items-center mb-2"
                     style="grid-template-columns: 2fr 1fr 80px 1fr 100px 40px">
                  <el-input v-model="item.name" size="default" placeholder="商品名称" />
                  <el-input v-model="item.spec" size="default" placeholder="规格" />
                  <el-input-number v-model="item.quantity" :min="1" controls-position="right" class="w-full" />
                  <el-input-number v-model="item.unitPrice" :min="0" :precision="2" controls-position="right" class="w-full" />
                  <div class="text-sm font-semibold text-right">{{ $n(itemSubtotal(item), 'currency') }}</div>
                  <el-button v-if="form.items.length > 1" link type="danger" :icon="Delete" @click="removeItem(i)" class="justify-self-center" />
                </div>
                <el-button link type="primary" :icon="Plus" @click="addItem">Add item</el-button>
              </template>
            </div>
          </div>
        </div>

        <!-- Tab: 附件 (M2M) -->
        <div v-show="activeTab === 'attachments'" class="hidden md:flex flex-col gap-4">
          <div class="bg-white rounded-btn border border-neutral-200 border-l-[3px] border-l-purple-600 p-5 md:p-6">
            <div class="text-sm font-semibold text-purple-700 mb-4 uppercase tracking-wide">附件</div>
            <div class="mt-4">
              <div class="text-sm text-neutral-600 mb-2">Attachments</div>
              <!-- Column headers (desktop only) -->
              <div class="hidden md:grid gap-2 px-1 py-2 border-b border-neutral-100 mb-2"
                   style="grid-template-columns: 2fr 1fr 1fr 40px">
                <span class="text-xs text-neutral-400">File Name *</span>
                <span class="text-xs text-neutral-400">Size</span>
                <span class="text-xs text-neutral-400">Uploaded</span>
                <span></span>
              </div>
              <template v-if="isView">
                <div v-for="(att, i) in form.attachments" :key="i"
                     class="grid gap-2 items-center py-2 border-b border-neutral-50 last:border-0"
                     style="grid-template-columns: 2fr 1fr 1fr">
                  <div class="text-sm text-neutral-950">{{ att.fileName || '-' }}</div>
                  <div class="text-sm text-neutral-500">{{ att.fileSize || '-' }}</div>
                  <div class="text-sm text-neutral-500">{{ att.uploadedAt || '-' }}</div>
                </div>
              </template>
              <template v-else>
                <div v-for="(att, i) in form.attachments" :key="i"
                     class="grid gap-2 items-center mb-2"
                     style="grid-template-columns: 2fr 1fr 1fr 40px">
                  <el-input v-model="att.fileName" size="default" placeholder="文件名称" />
                  <el-input v-model="att.fileSize" size="default" placeholder="如 2.5MB" />
                  <el-input v-model="att.uploadedAt" size="default" placeholder="日期" />
                  <el-button v-if="form.attachments.length > 1" link type="danger" :icon="Delete" @click="removeAttachment(i)" class="justify-self-center" />
                </div>
                <el-button link type="primary" :icon="Plus" @click="addAttachment">Add attachment</el-button>
              </template>
            </div>
          </div>
        </div>
      </template>

      <!-- ===== MOBILE + Simple form fallback: flat section cards ===== -->
      <div :class="tabs.length >= 2 ? 'md:hidden flex flex-col gap-4' : 'flex flex-col gap-4'">
        <!-- Section 1: 基本信息 (blue) -->
        <div class="bg-white rounded-btn border border-neutral-200 border-l-[3px] border-l-blue-600 p-5 md:p-6">
          <div class="text-sm font-semibold text-blue-700 mb-4 uppercase tracking-wide">基本信息</div>
          <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
            <el-form-item label="客户名称" prop="customer">
              <template v-if="isView"><div class="text-sm text-neutral-950 pt-1">{{ form.customer }}</div></template>
              <el-input v-else v-model="form.customer" placeholder="请输入客户名称" />
            </el-form-item>
            <el-form-item label="手机号" prop="phone">
              <template v-if="isView"><div class="text-sm text-neutral-950 pt-1">{{ form.phone }}</div></template>
              <el-input v-else v-model="form.phone" placeholder="请输入手机号" />
            </el-form-item>
            <el-form-item label="订单金额" prop="amount">
              <template v-if="isView"><div class="text-sm text-neutral-950 pt-1">{{ $n(form.amount, 'currency') }}</div></template>
              <el-input-number v-else v-model="form.amount" :min="0" :precision="2" controls-position="right" class="w-full" />
            </el-form-item>
          </div>
          <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mt-4">
            <el-form-item label="渠道">
              <template v-if="isView"><div class="text-sm text-neutral-950 pt-1">{{ form.channel }}</div></template>
              <el-select v-else v-model="form.channel" placeholder="请选择渠道" class="w-full">
                <el-option label="线上" value="线上" />
                <el-option label="门店" value="门店" />
                <el-option label="渠道商" value="渠道商" />
              </el-select>
            </el-form-item>
          </div>
        </div>

        <!-- Section 2: 商品清单 (purple, O2M) -->
        <div class="bg-white rounded-btn border border-neutral-200 border-l-[3px] border-l-purple-600 p-5 md:p-6">
          <div class="text-sm font-semibold text-purple-700 mb-4 uppercase tracking-wide">商品清单</div>
          <div class="mt-4">
            <div class="text-sm text-neutral-600 mb-2">Items</div>
            <div class="hidden md:grid gap-2 px-1 py-2 border-b border-neutral-100 mb-2"
                 style="grid-template-columns: 2fr 1fr 80px 1fr 100px 40px">
              <span class="text-xs text-neutral-400">Name *</span>
              <span class="text-xs text-neutral-400">Spec</span>
              <span class="text-xs text-neutral-400 text-center">Qty</span>
              <span class="text-xs text-neutral-400 text-right">Price</span>
              <span class="text-xs text-neutral-400 text-right">Subtotal</span>
              <span></span>
            </div>
            <template v-if="isView">
              <div v-for="(item, i) in form.items" :key="i"
                   class="grid gap-2 items-center py-2 border-b border-neutral-50 last:border-0"
                   style="grid-template-columns: 2fr 1fr 80px 1fr 100px">
                <div class="text-sm text-neutral-950">{{ item.name || '-' }}</div>
                <div class="text-sm text-neutral-500">{{ item.spec || '-' }}</div>
                <div class="text-sm text-neutral-950 text-center">{{ item.quantity }}</div>
                <div class="text-sm text-neutral-950 text-right">{{ $n(item.unitPrice, 'currency') }}</div>
                <div class="text-sm font-semibold text-neutral-950 text-right">{{ $n(itemSubtotal(item), 'currency') }}</div>
              </div>
            </template>
            <template v-else>
              <div v-for="(item, i) in form.items" :key="i"
                   class="grid gap-2 items-center mb-2"
                   style="grid-template-columns: 2fr 1fr 80px 1fr 100px 40px">
                <el-input v-model="item.name" size="default" placeholder="商品名称" />
                <el-input v-model="item.spec" size="default" placeholder="规格" />
                <el-input-number v-model="item.quantity" :min="1" controls-position="right" class="w-full" />
                <el-input-number v-model="item.unitPrice" :min="0" :precision="2" controls-position="right" class="w-full" />
                <div class="text-sm font-semibold text-right">{{ $n(itemSubtotal(item), 'currency') }}</div>
                <el-button v-if="form.items.length > 1" link type="danger" :icon="Delete" @click="removeItem(i)" class="justify-self-center" />
              </div>
              <el-button link type="primary" :icon="Plus" @click="addItem">Add item</el-button>
            </template>
          </div>
        </div>

        <!-- Section 3: 附件 (purple, M2M) -->
        <div class="bg-white rounded-btn border border-neutral-200 border-l-[3px] border-l-purple-600 p-5 md:p-6">
          <div class="text-sm font-semibold text-purple-700 mb-4 uppercase tracking-wide">附件</div>
          <div class="mt-4">
            <div class="text-sm text-neutral-600 mb-2">Attachments</div>
            <div class="hidden md:grid gap-2 px-1 py-2 border-b border-neutral-100 mb-2"
                 style="grid-template-columns: 2fr 1fr 1fr 40px">
              <span class="text-xs text-neutral-400">File Name *</span>
              <span class="text-xs text-neutral-400">Size</span>
              <span class="text-xs text-neutral-400">Uploaded</span>
              <span></span>
            </div>
            <template v-if="isView">
              <div v-for="(att, i) in form.attachments" :key="i"
                   class="grid gap-2 items-center py-2 border-b border-neutral-50 last:border-0"
                   style="grid-template-columns: 2fr 1fr 1fr">
                <div class="text-sm text-neutral-950">{{ att.fileName || '-' }}</div>
                <div class="text-sm text-neutral-500">{{ att.fileSize || '-' }}</div>
                <div class="text-sm text-neutral-500">{{ att.uploadedAt || '-' }}</div>
              </div>
            </template>
            <template v-else>
              <div v-for="(att, i) in form.attachments" :key="i"
                   class="grid gap-2 items-center mb-2"
                   style="grid-template-columns: 2fr 1fr 1fr 40px">
                <el-input v-model="att.fileName" size="default" placeholder="文件名称" />
                <el-input v-model="att.fileSize" size="default" placeholder="如 2.5MB" />
                <el-input v-model="att.uploadedAt" size="default" placeholder="日期" />
                <el-button v-if="form.attachments.length > 1" link type="danger" :icon="Delete" @click="removeAttachment(i)" class="justify-self-center" />
              </div>
              <el-button link type="primary" :icon="Plus" @click="addAttachment">Add attachment</el-button>
            </template>
          </div>
        </div>

        <!-- Section 4: 配送 & 备注 (cyan) -->
        <div class="bg-white rounded-btn border border-neutral-200 border-l-[3px] border-l-cyan-600 p-5 md:p-6">
          <div class="text-sm font-semibold text-cyan-700 mb-4 uppercase tracking-wide">配送 & 备注</div>
          <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
            <el-form-item label="配送方式">
              <template v-if="isView"><div class="text-sm text-neutral-950 pt-1">{{ form.deliveryMethod }}</div></template>
              <el-select v-else v-model="form.deliveryMethod" placeholder="请选择配送方式" class="w-full">
                <el-option label="快递" value="快递" />
                <el-option label="自提" value="自提" />
                <el-option label="同城送" value="同城送" />
              </el-select>
            </el-form-item>
          </div>
          <el-form-item label="配送地址" class="mt-4">
            <template v-if="isView"><div class="text-sm text-neutral-950 whitespace-pre-wrap pt-1">{{ form.address || '-' }}</div></template>
            <el-input v-else v-model="form.address" type="textarea" :rows="2" placeholder="请输入配送地址" />
          </el-form-item>
          <el-form-item label="备注" class="mt-4">
            <template v-if="isView"><div class="text-sm text-neutral-950 whitespace-pre-wrap pt-1">{{ form.remark || '-' }}</div></template>
            <el-input v-else v-model="form.remark" type="textarea" :rows="2" placeholder="备注信息（可选）" />
          </el-form-item>
        </div>
      </div>
    </el-form>
```

Note: The `<el-form>` closing tag and remaining template stay unchanged. Verify the `</el-form>` placement matches the nesting.

- [ ] **Step 4: Add mock data for attachments**

In the `onMounted` block, when loading mock data for view/edit mode, add `attachments`:

```typescript
onMounted(() => {
  if ((isEdit.value || isView.value) && route.params.id) {
    const order = __mockOrders.find(o => o.id === route.params.id)
    if (order) {
      form.customer = order.customer
      form.phone = order.phone
      form.amount = order.amount
      form.channel = order.channel
      form.items = order.items?.length ? [...order.items] : [defaultItem()]
      form.attachments = (order as any).attachments?.length
        ? [...(order as any).attachments]
        : [defaultAttachment()]
      form.address = (order as any).address || ''
      form.deliveryMethod = (order as any).deliveryMethod || ''
      form.remark = (order as any).remark || ''
      recordId = order.id
    }
  }
})
```

- [ ] **Step 5: Add sample attachments to one mock order**

In the `__mockOrders` array, add sample `attachments` to the first order:

```typescript
// Find the first mock order and add:
attachments: [
  { fileName: '合同.pdf', fileSize: '1.2MB', uploadedAt: '2026-06-10' },
  { fileName: '发票.png', fileSize: '450KB', uploadedAt: '2026-06-11' },
]
```

- [ ] **Step 6: Verify imports**

Ensure `WarningFilled` is imported from `@element-plus/icons-vue`:

```typescript
import { Plus, Delete, WarningFilled } from '@element-plus/icons-vue'
```

- [ ] **Step 7: Build and verify no TypeScript/compilation errors**

```bash
cd login-homepage-preview && npm run build
```

Expected: build succeeds with no errors.

- [ ] **Step 8: Commit**

```bash
git add login-homepage-preview/src/views/OrderFormView.vue
git commit -m "feat(demo): extend OrderFormView with tab layout, attachments M2M, and purple O2M cards"
```

---

### Task 7: Verify at all breakpoints via Playwright

**Files:**
- No file changes — verification only

- [ ] **Step 1: Start dev server**

```bash
cd login-homepage-preview && npm run dev
```

- [ ] **Step 2: Inspect OrderFormView at 1440px (desktop, create mode)**

Navigate to `http://localhost:5173/admin/orders/create`.

Checklist:
- [ ] Tab bar visible with 4 tabs: 基本信息, 配送 & 备注, 商品清单 (1), 附件 (1)
- [ ] "基本信息" tab active by default
- [ ] After adding items, the 商品清单 badge updates reactively
- [ ] Click through each tab — content switches, no validation triggered
- [ ] Purple accent stripe on 商品清单 and 附件 section cards
- [ ] Submit with empty required fields → auto-switch to first error tab
- [ ] Submit button: loading, disabled, and Toast on success

- [ ] **Step 3: Inspect OrderFormView at 1440px (view mode)**

Navigate to any order detail page.

Checklist:
- [ ] Tab bar still visible
- [ ] All content read-only
- [ ] No add/delete buttons on O2M/M2M grids
- [ ] Edit + Delete buttons in toolbar

- [ ] **Step 4: Inspect at 768px (tablet)**

Checklist:
- [ ] Tab bar visible (≥768px)
- [ ] 3-column grid working

- [ ] **Step 5: Inspect at 390px (mobile)**

Checklist:
- [ ] No tab bar visible
- [ ] All section cards stacked vertically with `gap-4`
- [ ] O2M items use compact card layout
- [ ] Form fields single column

- [ ] **Step 6: Commit if no issues found, or fix issues first**

If all inspections pass:
```bash
git add -A && git commit -m "chore: verification complete — tabs, mobile, all modes pass"
```

If issues found, fix them and re-inspect before committing.

---

### Task 8: Cleanup temporary files

**Files:**
- Delete: `*.png` in project root
- Delete: `.playwright-mcp/` directory

- [ ] **Step 1: Delete Playwright screenshots and temp directory**

```bash
cd "D:\projects\UI-UX-design" && rm -f *.png && rm -rf .playwright-mcp/
```

- [ ] **Step 2: Push all changes**

```bash
git push origin main
```

- [ ] **Step 3: Push skill subtree (if skill files changed)**

```bash
git subtree push --prefix=.claude/skills/ui-ux-agent-designer skill main
```

---

## Plan Self-Review

**Spec coverage check:**
- Tab bar placement (between toolbar and form) → Task 5 (generation-rules), Task 6 (OrderFormView)
- Decision matrix (when tabs vs no tabs) → Task 5 (generation-rules)
- O2M badge with count → Task 5 (generation-rules), Task 6 (OrderFormView)
- Purple accent for O2M/M2M → Task 1 (design-tokens), Task 6 (OrderFormView)
- Free tab switching, no validation → Task 2 (interaction-rules), Task 5 (generation-rules)
- Cross-tab validation + error tab → Task 2 (interaction-rules), Task 5 (generation-rules), Task 6 (OrderFormView)
- Three-mode integration → Task 5 (generation-rules), Task 6 (OrderFormView)
- Mobile collapse to flat sections → Task 2 (interaction-rules), Task 5 (generation-rules), Task 6 (OrderFormView)
- Component mapping → Task 3 (component-system)
- Review checklist → Task 4 (review-checklist)
- Demo validation → Task 6 (OrderFormView), Task 7 (verification)

**Placeholder scan:** No TBD, TODO, or vague references. All code is explicit.

**Type consistency:** `TabDefinition` interface used consistently across Tasks 5 and 6. `tabs` computed, `activeTab` ref, `handleTabChange` function signatures match.
